apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appbase.fullname" . }}-test-connection"
  labels:
    {{- include "appbase.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appbase.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
