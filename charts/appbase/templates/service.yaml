apiVersion: v1
kind: Service
metadata:
  name: {{ include "appbase.fullname" . }}
  labels:
    {{- include "appbase.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "appbase.selectorLabels" . | nindent 4 }}
